{
  "name": "koop-provider-csv",
  "version": "3.0.1",
  "description": "Koop provider for CSV",
  "main": "src/index.js",
  "scripts": {
    "test": "tape 'test/**/*.test.js'",
    "start": "cross-env NODE_ENV=dev koop serve -p 8080"
  },
  "author": "Haoliang Yu",
  "license": "MIT",
  "keywords": [
    "koopjs",
    "plugin",
    "provider",
    "csv"
  ],
  "dependencies": {
    "autodetect-decoder-stream": "^1.0.3",
    "config": "^3.1.0",
    "csv-reader": "^1.0.3",
    "is-url-superb": "^3.0.0",
    "node-fetch": "^2.6.0"
  },
  "devDependencies": {
    "@koopjs/cli": "^0.4.0",
    "cross-env": "^5.2.0",
    "fetch-mock": "^7.3.3",
    "husky": "^2.3.0",
    "lint-staged": "^8.1.7",
    "prettier": "^1.17.1",
    "proxyquire": "^2.1.0",
    "tape": "^4.10.2"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged",
      "pre-push": "npm test"
    }
  },
  "lint-staged": {
    "*.{js,json,md}": [
      "prettier --write",
      "git add"
    ]
  }
}
